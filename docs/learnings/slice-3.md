# Slice 3 reflection

## Learned

News discovery is retrieval, not generation. Queries come from strong and credible persona terms, never from risky topics. Missing URL, date, or keyword overlap is a software rejection.

Preferred domains are a first pass. An empty quality-domain result may widen, but the overlap filter still runs.

Quoted NewsAPI phrases must be short. Long persona labels like "modern frontend frameworks (Vue.js, React.js)" return zero hits. Compact to searchable terms and score overlap by tokens.

## Reviewer notes

- Do not invent headlines when the provider is down.
- Empty overlap is an empty state, not a reason to loosen authority.
- Semantic “Why this post?” stays in Slice 4.

## Next

Slice 4 ranks survivors with hybrid relevance and turns them into content opportunities.
